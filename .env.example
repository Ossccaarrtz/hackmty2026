# Copia este archivo a .env y llena los valores reales. Nunca subas el .env real al repo.

# --- Backend (Lambda / scripts de seed) ---
NESSIE_API_KEY=pide-la-clave-al-equipo
GEMINI_API_KEY=pide-la-clave-al-equipo
GEMINI_MODEL=gemini-3.1-flash-lite

# --- Frontend (React) ---
# Endpoint real del backend, ya desplegado y probado:
VITE_API_BASE_URL=https://qj0vumzrfa.execute-api.us-east-1.amazonaws.com
